# Ejemplo Python/Django en docker
Basado en clases anteriores

## Como correr la aplicación?
Para correr la aplicación usa el siguiente comando dentro de la carpeta `clase_5_python`:
```
docker-compose up
```
